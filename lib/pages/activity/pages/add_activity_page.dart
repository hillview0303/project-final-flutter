import 'package:flutter/material.dart';

class AddActivityPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('활동 추가하기')),
      body: Center(
        child: Text('활동 추가하기'),
      ),
    );
  }
}