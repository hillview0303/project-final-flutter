import 'package:flutter/material.dart';

class ChangeWeightDetailPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('체중')),
      body: Center(
        child: Text('체중'),
      ),
    );
  }
}